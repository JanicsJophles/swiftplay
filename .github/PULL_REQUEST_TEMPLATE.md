## What & why

<!-- What does this change, and what problem does it solve? Link any issue. -->

## How it was tested

<!--
CI only build-checks (the dogfood suite needs a Mac with Accessibility granted —
see CONTRIBUTING.md). So tell us what you ran locally:
-->
- [ ] `env -u TOOLCHAINS xcrun --toolchain XcodeDefault swift build --product swiftplay` succeeds
- [ ] Ran against a real app (which one? which commands?):
- [ ] Updated docs (README) if behavior or commands changed

## Notes for reviewers

<!-- AX-tree quirks discovered, gotchas, anything non-obvious. Worth adding to examples/rackmind-macos/FINDINGS.md? -->
